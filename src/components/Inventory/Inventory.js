import React from 'react';

const Inventory = () => {
    return (
        <div>
            <h2>This is Inventory</h2>
            {
                console.log("this is inventory")
            }
        </div>
    );
};

export default Inventory;